{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="page-header">
        <span>
            Exames
        </span>

        <span style="float: right"><a href="{{ path("exame_new") }}" class="btn btn-primary">
                    Novo Exame
                </a>
            </span>
    </h1>


    <table class="table">
        <thead>
        <tr>
            <th>Descrição</th>
            <th>Ano</th>
            <th>Ações</th>
        </tr>
        </thead>
        <tbody>
        {% for exame in exames %}
            <tr>
                <td>{{ exame.descricao }}</td>
                <td>{{ exame.ano }}</td>
                <td>

                    <a href="{{ path('exame_edit', { 'id': exame.id }) }}"
                       class="btn btn-default" data-toggle="tooltipe"
                       title="Editar exame">
                        Editar
                    </a>

                    <a href="{{ path('arquivo_index', { 'exame':exame.id }) }}"
                       class="btn btn-info" data-toggle="tooltipe"
                       title="Editar exame">
                        Planilhas
                    </a>

                    {#<li>#}
                    {#<a href="{{ path('arquivo_index', { 'exame':exame.id }) }}">planilhas</a>#}
                    {#</li>#}
                    {#</ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#<div class="well">#}
        <a href="#"
           class="btn btn-primary" data-toggle="tooltipe"
           title="Editar exame">
            Realizar extração e carga
        </a>
    {#</div>#}
{% endblock %}
