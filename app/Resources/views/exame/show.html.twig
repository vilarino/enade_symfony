{% extends 'base.html.twig' %}

{% block body %}
    <h1>Exame</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exame.id }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ exame.descricao }}</td>
            </tr>
            <tr>
                <th>Ano</th>
                <td>{{ exame.ano }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('exame_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('exame_edit', { 'id': exame.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
